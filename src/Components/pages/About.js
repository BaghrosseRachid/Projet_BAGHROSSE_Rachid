import React from 'react'

export default function About(props) {
    return (
        <div>
           <h2> this is my first application created using react js </h2> 
            
                {/*  <h5>{props.match.params.id}</h5>
                 <h5>{props.match.params.name}</h5> */}
              
        </div>
    )
}
